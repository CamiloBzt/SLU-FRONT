mutation saveEndorsementDocument($subscription_id: ID!, $doc_s3: String!) {
  saveEndorsementDocument(subscription_id: $subscription_id, doc_s3: $doc_s3) {
    statusCode
    message
    response
    error
  }
}
